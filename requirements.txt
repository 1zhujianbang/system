# 最简核心依赖 - 仅包含必需的顶级包
# 其他依赖会作为传递依赖自动安装

# Web框架和UI
streamlit

# 数据处理
pandas

# 配置和环境
pyyaml
python-dotenv

# 异步操作
aiohttp
aiofiles

# 图处理
networkx
pyvis

# AI/LLM
openai

# 数据验证
pydantic

# 安全
cryptography

# 系统工具
watchdog
psutil

# 可视化
altair

# 图数据库
neo4j

# 分布式任务处理
celery
redis

# 因果推断
causal-learn
dowhy

# 文本相似度算法（可选，提升实体合并准确度）
# 如果不安装，会降级到标准库的 difflib.SequenceMatcher
jellyfish  # Jaro-Winkler、Levenshtein 等高级字符串相似度算法

# 语义相似度（可选，支持跨语言实体匹配）
# 注意：sentence-transformers 模型较大（~500MB），首次加载需下载
# sentence-transformers  # 取消注释以启用语义匹配

# GDELT数据获取库
gdelt

# 分布式数据处理
dask[dataframe]

pytest

